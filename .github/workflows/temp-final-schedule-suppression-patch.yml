name: Temporary backend final Schedule patch

on:
  push:
    branches:
      - fix/schedule-deletion-final
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  patch:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo paused
